import React from "react";
import { View } from "react-native";

const RenderSepartor = props => {
    return (
        <View
            style={{
                height: 1,
                width: '100%',
                backgroundColor: '#CED0CE',
            }}
        />
    );
}

export default RenderSepartor;